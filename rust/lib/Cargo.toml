[package]
name = "rtcl-lib"
version = "0.1.0"
edition = "2024"

[dependencies]
jelly-lib = "0.2.2"
nix = { version ="0.30.1", features = ["fs", "ioctl"], optional = true }

[features]
default = ["std"]
std = ["nix"]
